const topbar=document.getElementById('topbar');window.addEventListener('scroll',()=>{if(topbar){topbar.classList.toggle('solid',window.scrollY>80)}});const obs=new IntersectionObserver((e)=>{e.forEach(x=>{if(x.isIntersecting){x.target.classList.add('visible');obs.unobserve(x.target)}})},{threshold:.16});document.querySelectorAll('.reveal,.card,.species-card,.step,.photo-card,.video-card,.time').forEach(el=>obs.observe(el));