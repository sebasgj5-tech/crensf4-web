
const topbar=document.querySelector('.topbar');function onScroll(){if(scrollY>35)topbar?.classList.add('scrolled');else topbar?.classList.remove('scrolled')}addEventListener('scroll',onScroll);onScroll();const io=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('.reveal,.card,.species-card,.step,.media-card,.time').forEach(el=>{el.classList.add('reveal');io.observe(el)});
